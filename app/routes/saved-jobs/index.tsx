import PageHeader from "~/components/page-header";

export default function SavedJobs() {
  return (
    <PageHeader
      title="Saved Jobs"
      description="This is the saved jobs page. Your saved job listings will be displayed here."
    />
  );
}
